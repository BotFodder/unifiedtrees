--- lib/html_tree.dist	2013-12-09 13:08:28.000000000 -0500
+++ lib/html_tree.php	2013-12-09 13:08:54.000000000 -0500
@@ -505,6 +505,11 @@
 	PERSERVESTATE = 1
 	HIGHLIGHT = 1
 	<?php
+$utpi = db_fetch_cell("SELECT status FROM plugin_config WHERE directory='unifiedtrees'");
+if(read_config_option('unifiedtrees_use_ut') == 'on' &&
+   $utpi == 1) {
+	include_once($config["base_path"]. "/plugins/unifiedtrees/unifiedtrees.php");
+}else{
 	/* get current time */
 	list($micro,$seconds) = explode(" ", microtime());
 	$current_time = $seconds + $micro;
@@ -528,6 +533,7 @@
 	for ($i = 2; $i <= $total_tree_items; $i++) {
 		print $dhtml_tree[$i];
 	}
+}
 	?>
 	//-->
 	</script>
